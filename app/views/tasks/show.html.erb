<h1>id: <%= @tasks.id %> のタスク詳細ページ</h1>

<p><%= @task.status %></p>
<p><%= @task.content %></p>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @tasks.id %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @tasks.status %></td>
  </tr>
  <tr>
    <th>メッセージ</th>
    <td><%= @tasks.content %></td>
  </tr>
</table>

<%= link_to '一覧に戻る', edit_task_path(task) %>
<%= link_to 'このメッセージを編集する', edit_task_path(@task), class: 'btn btn-default' %>
<%= link_to 'このメッセージを削除する', @task, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>