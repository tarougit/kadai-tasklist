<h1>タスク新規作成ページ</h1>

<%= render 'form', tasks: @tasks %>

<%= form_for(@task) do |f| %>
  <%= f.label :statu, 'ステータス' %>
  <%= f.text_field :status %>

  <%= f.submit '投稿' %>
<% end %>

<%= form_for(@task) do |f| %>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_field :content %>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>